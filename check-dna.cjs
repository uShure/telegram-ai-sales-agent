const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('./courses.db'); db.all("SELECT id, title, url FROM Course WHERE title LIKE '%ДНК%' OR url LIKE '%dna%'", (err, rows) => { if (err) { console.error(err); } else { console.log('Найдены курсы с ДНК:'); rows.forEach(row => console.log(`ID: ${row.id}, Название: ${row.title}, URL: ${row.url}`)); } db.close(); });
