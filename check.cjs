const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('./courses.db'); db.get("SELECT * FROM Course WHERE id = '18'", (err, row) => { if(row) { console.log('Курс: ' + row.title); console.log('Цена: ' + row.price + ' руб'); console.log('Описание: ' + row.description); console.log('Длительность: ' + row.duration); console.log('Формат: ' + row.format); console.log('Модули: ' + row.modules); console.log('Документ: ' + row.certificateType); console.log('Ссылка: ' + row.url); } db.close(); });
