const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('./courses.db'); db.get("SELECT * FROM Course WHERE id = '11'", (err, row) => { if (row) { console.log('\n📚 ДНК ЦВЕТА - КУРС ПО КОЛОРИСТИКЕ'); console.log('═══════════════════════════════════════════'); console.log('💰 Цена: ' + (row.price ? row.price.toLocaleString('ru-RU') + ' ₽' : 'не указана')); console.log('📚 Формат: ' + row.format); console.log('⏱️ Длительность: ' + (row.duration || 'не указана')); console.log('👨‍🏫 Преподаватели: ' + row.instructor); console.log('🏷️ Категория: ' + row.category); console.log('🔗 Ссылка: ' + row.url); if (row.description) console.log('\n📝 Описание:\n' + row.description); } db.close(); });
